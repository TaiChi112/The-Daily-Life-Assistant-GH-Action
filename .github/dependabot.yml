version: 2
updates:
  - package-ecosystem: "npm" # ใช้สำหรับ Bun/Node.js
    directory: "/"
    schedule:
      interval: "weekly"     # ตรวจแค่อาทิตย์ละครั้ง (วันจันทร์เช้า) ไม่ต้อง spam ทุกวัน
      day: "monday"
      time: "09:00"
      timezone: "Asia/Bangkok" # เวลาไทย
    
    # 🔥 MAX POWER 1: Grouping
    # แทนที่จะเปิด 10 PRs แยกกัน, ให้รวม Library ทุกตัวไว้ใน PR เดียว
    # ทำให้เรากด Merge แค่ทีเดียวจบ!
    groups:
      dependencies:
        patterns:
          - "*" 
    
    # จัดระเบียบ
    open-pull-requests-limit: 10
    target-branch: "main"
    labels:
      - "dependencies"
      - "automated-pr"
    
    # สั่งให้ Assign PR มาที่คุณอัตโนมัติ (แก้เป็นชื่อ GitHub คุณ)
    assignees:
      - "TaiChi112"
      
    # ปรับแต่ง Commit Message ให้ดูโปร
    commit-message:
      prefix: "chore(deps)"
      prefix-development: "chore(deps-dev)"
      include: "scope"